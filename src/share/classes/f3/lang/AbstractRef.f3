package f3.lang;
import f3.math.*;

public abstract class AbstractRef of a is AbstractConstRef of a, Ref of a {

    public function toggle from (this is AbstractRef of Boolean) to () {
        const r = this;
        r.set(not r.get())
    }
}

