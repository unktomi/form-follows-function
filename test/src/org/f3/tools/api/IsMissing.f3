// source for which a missing identifier will be filled in.
// With some other stuff, to test that the other stuff isn't marked as missing
class Foo {
  function bleep() {
    for (x in [1..5]) {
        var y = indexof x;
        var z = indexof;  // missing identifier
    }
  }
}
