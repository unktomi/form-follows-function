/**
 * VSGC-2813 : For-loop over range generates unnecessary garbage objects on heap
 *
 * Indexof usage
 *
 * @test
 * @run
 */

for (xxx in [10 .. 19.0 step 2]) {
   println(xxx);
   println(indexof xxx);
}
