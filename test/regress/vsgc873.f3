/*
 * Invoking getClass() on a primitive value should used its boxed type.
 * (getClass is a special case because it returns a generioc class.)
 *
 * @test
 * @run
 */

var i: Integer = 1;
var c = i.getClass();
java.lang.System.out.println("{i}.getClass() ==> {c}");

