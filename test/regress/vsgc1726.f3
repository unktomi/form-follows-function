/**
 * regression test:   NPE throws with for loop containing if block having break statements
 * @test
 * @run
 */

import java.lang.*;

for(i in [1..100]){
    if(i>15) break;
    System.out.println(i);
}
