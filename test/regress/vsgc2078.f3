/*
 * @test
 * @run
 */

import java.lang.System;
def tenInts = [1..10];
def subsequence = tenInts[0..3.2]; // Should warn, but compile.
for (x in subsequence)
  System.out.println("subsequence[{indexof x}]={x}");
