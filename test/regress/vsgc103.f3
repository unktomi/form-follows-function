/*
 * @test
 * @run
 */
var b1 = Boolean.TRUE;
var b2 = false;
println("{b1} (boxed) instanceof Boolean => {b1 instanceof Boolean}");
println("{b2} (unboxed) instanceof Boolean => {b2 instanceof Boolean}");
var t1 = bind b1 instanceof Boolean;
println("{t1} (bind boxed) instanceof Boolean => {t1 instanceof Boolean}");
//FIXME doesn't compile.
//var t2 = bind b2 instanceof Boolean;
//println("{t2} (bind unboxed) instanceof Boolean => {t2 instanceof Boolean}");
var i1 = Integer.valueOf(12);
var i2 = 1;
println("{i1} (boxed) instanceof Integer => {i1 instanceof Integer}");
println("{i2} (unboxed) instanceof Integer => {i2 instanceof Integer}");

